apiVersion: v1
kind: Pod
metadata:
  name: config-demo
spec:
  containers:
  - name: alpine
    image: alpine:latest
    command: ["sleep", "3600"]
    envFrom:
    - configMapRef:
        name: app-config
    - secretRef:
        name: db-credentials
